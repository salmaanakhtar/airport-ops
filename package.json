{
  "name": "airport-ops",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AIRPORT // OPS — deeply simulated airport ground-operations management game",
  "scripts": {
    "dev": "vite --port 8191 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 8192 --strictPort",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
